# GYPPORT canonical physical location registry
#
# Single source of truth for WHERE a logical GYPPORT storage role currently lives on this machine.
# Architecture, agent governance and tooling refer to the logical identifier GYPPORT_STORAGE;
# only this registry knows the physical path.
#
# Moving GYPPORT_STORAGE to a NAS is a configuration change: update the value below, validate that
# the new root exists, and continue. No governance, memory, BoxGhost, CURRENT_STEP or Brain
# document changes.
#
# Reading rules:
#   - values are literal filesystem paths; a backslash is a backslash and no escape processing
#     is applied when reading this file;
#   - a value may be a local path or a UNC path such as \SERVER\share\folder;
#   - prefer a stable UNC path over a machine-dependent mapped drive letter;
#   - exactly one active value per key;
#   - if the resolved root does not exist, the caller reports GYPPORT_STORAGE_AVAILABLE=NO and
#     stops. It never creates a replacement root and never falls back to a temporary folder,
#     the Desktop or Downloads.
#
# Logical identifier: GYPPORT_STORAGE
# Contract: Fabric/Knowledge/00-GYPPORT-UNIVERSE/GYPPORT_MEMORY_ARCHITECTURE.md

GYPPORT_STORAGE_ROOT=D:\NZXTG7\GYPPORT\GYPPORT ERP\GYPPORT-Storage
